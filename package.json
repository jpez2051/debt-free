{
  "name": "debt-free",
  "private": true,
  "version": "0.5.18",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "test": "node --test",
    "verify:version": "node scripts/check-version.js",
    "build": "vite build",
    "check": "npm test && npm run verify:version && npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest",
    "lucide-react": "latest"
  },
  "devDependencies": {}
}
