function replace(code,before,after){if(!code.includes(before))throw new Error(`v0.9.9 missing ${before.slice(0,100)}`);return code.replace(before,after)}
function line(code,prefix,after){const found=code.split('\n').find(value=>value.startsWith(prefix));if(!found)throw new Error(`v0.9.9 missing line ${prefix}`);return code.replace(found,after)}

export function transformAppV099(source){
  let code=`import CategoryFields from './CategoryFields.jsx'
import { CATEGORY_GROUPS, DEFAULT_EXPENSE_CATEGORY, DEFAULT_BILL_CATEGORY, TRANSFER_PURPOSES, categoryLabel, categorySelection, isSavedOrInvestedTransfer, isDiscretionaryEntry, merchantProfiles } from './lib/categories.js'
${source}`
  code=replace(code,"const VERSION='0.9.8'","const VERSION='0.9.9'")
  code=line(code,'const categories=',"const categories=CATEGORY_GROUPS.map(group=>group.name)")
  code=line(code,' const spend='," const spend=purchases.reduce((s,t)=>s+Number(t.amount),0),income=incomes.reduce((s,t)=>s+Number(t.amount),0),savedInvested=transfers.filter(isSavedOrInvestedTransfer).reduce((s,t)=>s+Number(t.amount),0),otherTransferred=transfers.filter(t=>!t.toAccountId&&!isSavedOrInvestedTransfer(t)).reduce((s,t)=>s+Number(t.amount),0),netSpend=spendingEntries.reduce((s,t)=>s+Number(t.amount),0),paid=reportingPayments.reduce((s,p)=>s+Number(p.amount),0),billsPaid=reportingBillPayments.reduce((s,p)=>s+Number(p.amount),0)")
  code=line(code,' const merchantSuggestions='," const merchantSuggestions=useMemo(()=>merchantProfiles(allPurchases),[data.transactions])\n const selectMerchant=value=>{const profile=merchantSuggestions.find(item=>item.name.toLocaleLowerCase()===value.trim().toLocaleLowerCase());setForm(previous=>({...previous,merchant:value,...(profile?{category:profile.category,subcategory:profile.subcategory}:{})}))}")
  code=line(code,' const discretionary='," const discretionary=Math.max(0,spendingEntries.filter(isDiscretionaryEntry).reduce((sum,item)=>sum+Number(item.amount),0)),top=byCategory[0]")
  code=line(code,' const openPurchase='," const openPurchase=t=>{setForm(t?{...t,...categorySelection(t),date:transactionDay(t)}:{date:dateValue(),historical:false,...DEFAULT_EXPENSE_CATEGORY});setModal('purchase')}")
  code=line(code,' const openTransfer='," const openTransfer=t=>{const source=checking[0]||cashAccounts[0],destination=cashAccounts.find(a=>a.type==='savings'&&a.id!==source?.id);setForm(t?{...t,date:transactionDay(t),transferPurpose:t.transferPurpose||(t.toAccountId&&account(t.toAccountId)?.type==='savings'?'Savings contribution':'Other transfer')}:{date:dateValue(),historical:false,accountId:source?.id||'',toAccountId:destination?.id||'',merchant:destination?.name||'',transferPurpose:destination?'Savings contribution':'Other transfer'});setModal('transfer')}")
  code=line(code,' const openBill='," const openBill=b=>{setForm(b?{frequency:'monthly',autopay:false,...b,...categorySelection(b)}:{active:true,autopay:false,frequency:'monthly',dueDay:1,nextDueDate:dateValue(),...DEFAULT_BILL_CATEGORY,accountId:checking[0]?.id||cashAccounts[0]?.id||cards[0]?.id||''});setModal('bill')}")
  code=line(code,' const openBillPayment='," const openBillPayment=(b,selected)=>{const candidates=obligations.filter(x=>x.billId===b.id),obligation=candidates.find(x=>x.remaining>0)||candidates.at(-1),cycle=selected||data.billCycles.find(c=>c.id===obligation?.cycleId);if(!cycle)return;setForm({billId:b.id,billName:b.name,...categorySelection(b),cycleId:cycle.id,amount:cycleTotals(cycle,data.billPayments).remaining,date:dateValue(),bankId:b.accountId||checking[0]?.id||cashAccounts[0]?.id||'',historical:false});setModal('billPayment')}")
  code=code.replaceAll('data.transactions.filter(t=>t.accountId===old.id)','data.transactions.filter(t=>t.accountId===old.id||t.toAccountId===old.id)')
  code=code.replaceAll('data.transactions.filter(t=>t.accountId===a.id)','data.transactions.filter(t=>t.accountId===a.id||t.toAccountId===a.id)')
  code=replace(code,"t.kind==='transfer'?'External transfer':t.kind==='refund'?`Refund · ${t.category}`:t.category","t.kind==='transfer'?(t.transferPurpose||'Other transfer'):t.kind==='refund'?`Refund · ${categoryLabel(t)}`:categoryLabel(t)")
  code=replace(code,"right={`${(t.kind==='income'||t.kind==='refund')?'+':'-'}${money2.format(t.amount)}`}","right={`${t.kind==='transfer'&&t.toAccountId?'↔ ':(t.kind==='income'||t.kind==='refund')?'+':'-'}${money2.format(t.amount)}`}")
  code=replace(code,"sub={`${new Date(`${transactionDay(t)}T12:00:00`).toLocaleDateString()} · ${account(t.accountId)?.name||'Unknown'}`}","sub={`${new Date(`${transactionDay(t)}T12:00:00`).toLocaleDateString()} · ${account(t.accountId)?.name||'Unknown'}${t.kind==='transfer'?` → ${t.toAccountId?account(t.toAccountId)?.name||'Unknown':t.merchant}`:''}`}")
  code=replace(code,"<input list={modal==='purchase'?'merchant-suggestions':undefined} autoComplete=\"off\" value={form.merchant||''} onChange={e=>setForm({...form,merchant:e.target.value})}/>","<input list={modal==='purchase'?'merchant-suggestions':undefined} autoComplete=\"off\" value={form.merchant||''} onChange={e=>modal==='purchase'?selectMerchant(e.target.value):setForm({...form,merchant:e.target.value})}/>")
  code=replace(code,'{merchantSuggestions.map(name=><option value={name} key={name}/>)}</datalist>','{merchantSuggestions.map(item=><option value={item.name} key={item.name}/>)}</datalist>')
  code=replace(code,'{merchantSuggestions.slice(0,5).map(name=><button type="button" key={name} onClick={()=>setForm({...form,merchant:name})}>{name}</button>)}','{merchantSuggestions.slice(0,5).map(item=><button type="button" key={item.name} onClick={()=>selectMerchant(item.name)}>{item.name}</button>)}')
  code=replace(code,"{modal==='purchase'&&<Field label=\"Category\"><select value={form.category||'Groceries'} onChange={e=>setForm({...form,category:e.target.value})}>{categories.map(c=><option key={c}>{c}</option>)}</select></Field>}","{modal==='purchase'&&<><CategoryFields category={form.category} subcategory={form.subcategory} onChange={(category,subcategory)=>setForm({...form,category,subcategory})}/><p className=\"modal-note category-note\">Choose what you bought, not only the store. For example, a convenience-store snack is Food &amp; Drink; gasoline is Transportation.</p></>}")
  code=line(code,"   {modal==='transfer'&&",`   {modal==='transfer'&&<><Field label="Date"><DateInput value={form.date||dateValue()} onChange={e=>setForm({...form,date:e.target.value})}/></Field><Field label="Transfer from"><select required value={form.accountId||''} onChange={e=>{const same=e.target.value===form.toAccountId;setForm({...form,accountId:e.target.value,toAccountId:same?'':form.toAccountId,merchant:same?'':form.merchant,transferPurpose:same?'Other transfer':form.transferPurpose})}}><option value="">Choose account</option>{cashAccounts.map(a=><option value={a.id} key={a.id}>{a.name} — {money.format(a.balance)}</option>)}</select></Field><Field label="Transfer to"><select value={form.toAccountId||''} onChange={e=>{const destination=account(e.target.value);setForm({...form,toAccountId:e.target.value,merchant:destination?.name||'',transferPurpose:destination?.type==='savings'?'Savings contribution':destination?'Account transfer':'Other transfer'})}}><option value="">Outside tracked accounts (for example Acorns)</option>{cashAccounts.filter(a=>a.id!==form.accountId).map(a=><option value={a.id} key={a.id}>{a.name} — {money.format(a.balance)}</option>)}</select></Field>{!form.toAccountId&&<Field label="Outside destination"><input required placeholder="Acorns" value={form.merchant||''} onChange={e=>setForm({...form,merchant:e.target.value})}/></Field>}<Field label="Purpose"><select required value={form.transferPurpose||'Other transfer'} onChange={e=>setForm({...form,transferPurpose:e.target.value})}>{TRANSFER_PURPOSES.map(purpose=><option key={purpose}>{purpose}</option>)}</select></Field><Field label="Transfer amount"><input required type="number" inputMode="decimal" step=".01" min=".01" value={form.amount||''} onChange={e=>setForm({...form,amount:e.target.value})}/></Field><p className="modal-note transfer-note">Tracked-account transfers update both balances. Savings and investment contributions are reported separately; no transfer counts as spending.</p></>}`)
  code=replace(code,"<Field label=\"Category\"><select value={form.category||'Utilities'} onChange={e=>setForm({...form,category:e.target.value})}>{categories.map(c=><option key={c}>{c}</option>)}</select></Field>","<CategoryFields category={form.category} subcategory={form.subcategory} onChange={(category,subcategory)=>setForm({...form,category,subcategory})}/>")
  code=replace(code,'<CategoryFields category={form.category} subcategory={form.subcategory} onChange={(category,subcategory)=>setForm({...form,category,subcategory})}/></div><Field label="Payment method">','</div><CategoryFields category={form.category} subcategory={form.subcategory} onChange={(category,subcategory)=>setForm({...form,category,subcategory})}/><Field label="Payment method">')
  code=replace(code,'<PeriodPicker value={reportPeriod} onChange={setReportPeriod}/><section className="spending-controls"','<PeriodPicker value={reportPeriod} onChange={setReportPeriod}/><section className="stats spending-allocation-stats"><Stat label="Net spending" value={money.format(netSpend)} note="Expenses and recurring charges minus refunds"/><Stat label="Saved & invested" value={money.format(savedInvested)} note="Transfers excluded from spending totals"/></section><section className="spending-controls"')
  code=replace(code,'<option value="category">Category</option><option value="merchant">Merchant</option>','<option value="category">Main category</option><option value="subcategory">Subcategory</option><option value="merchant">Merchant</option>')
  code=replace(code,"<p className=\"muted\">Dining, shopping, subscriptions, entertainment and travel currently logged.</p>","<p className=\"muted\">Restaurants, fast food, snacks, shopping, subscriptions, entertainment and travel currently logged.</p>")
  code=replace(code,"<FlowBars rows={[['Income',income],['External transfers',transferred],['Expenses',spend],['Card payments',paid],['Bills/charges logged',billsPaid]]}/>","<FlowBars rows={[['Income',income],['Saved & invested',savedInvested],['Other outside transfers',otherTransferred],['Expenses',spend],['Card payments',paid],['Bills/charges logged',billsPaid]]}/>")
  code=code.replaceAll('${bill.name} · ${bill.category}','${bill.name} · ${categoryLabel(bill)}')
  code=line(code,'function SpendingBreakdown(',"function SpendingBreakdown({purchases,accounts,groupBy,sortBy}){const groups=useMemo(()=>{const accountNames=Object.fromEntries(accounts.map(a=>[a.id,a.name])),grouped=new Map();purchases.forEach(t=>{const category=t.category?.trim()||'Other',subcategory=t.subcategory?.trim()||'Other',merchant=t.merchant?.trim()||'Other',account=accountNames[t.accountId]||'Unknown account',name=groupBy==='category'?category:groupBy==='subcategory'?subcategory:groupBy==='merchant'?merchant:account,detail=groupBy==='category'?`${subcategory} · ${merchant} · ${account}`:groupBy==='subcategory'?`${category} · ${merchant} · ${account}`:groupBy==='merchant'?`${category} · ${subcategory} · ${account}`:`${category} · ${subcategory} · ${merchant}`,item=grouped.get(name)||{name,total:0,last:0,details:new Map()};item.total+=Number(t.amount);item.last=Math.max(item.last,new Date(t.date).getTime()||0);item.details.set(detail,(item.details.get(detail)||0)+Number(t.amount));grouped.set(name,item)});const rows=[...grouped.values()].map(item=>({...item,details:[...item.details].map(([name,total])=>({name,total})).sort((a,b)=>b.total-a.total||a.name.localeCompare(b.name))}));return rows.sort((a,b)=>sortBy==='name'?a.name.localeCompare(b.name):sortBy==='recent'?b.last-a.last||b.total-a.total:b.total-a.total||a.name.localeCompare(b.name))},[purchases,accounts,groupBy,sortBy]);const title={category:'Main category',subcategory:'Subcategory',merchant:'Merchant',account:'Account'}[groupBy];return <Panel title={`By ${title}`} kicker=\"WHERE MONEY WENT\">{groups.length?groups.map(group=><article className=\"spending-group\" key={group.name}><div className=\"spending-group-head\"><strong>{group.name}</strong><b>{money2.format(group.total)}</b></div><div className=\"spending-detail-list\">{group.details.map(detail=><div key={detail.name}><span>{detail.name}</span><b>{money2.format(detail.total)}</b></div>)}</div></article>):<Empty text=\"Log an expense to see its spending breakdown.\"/>}</Panel>}")
  return code
}
